header <- function() {
	dashboardHeader(
	#	title = tagList(	span(class = 'logo-lg',a("ActivtityInfo Quality",style="color:white !important",href='https://rstudio.unhcr.org/')),	)
		title =  "ActivtityInfo Quality" 
	
	)
}
